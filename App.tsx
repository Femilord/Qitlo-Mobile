/**
 * This file is no longer the app entry point.
 *
 * Qitlo Mobile uses expo-router for file-based routing — see package.json's
 * "main" field ("expo-router/entry") and the app/ directory for the real
 * route tree. This file is kept as a stub so any tooling that still expects
 * an App.tsx at the project root doesn't error.
 */

export default function App() {
  return null;
}
